@*@{
    ViewData["Title"] = "About";
}
<h2>@ViewData["Title"]</h2>
<h3>@ViewData["Message"]</h3>*@

<center>
    <form action="/" method="post">
        <div class="row">
            <div class="col-md-12">
                <h3>Money Tracker</h3>
                <table class="table">
                    <tbody>
                        @*@foreach (var item in Model)
                            {*@
                        <tr>
                            <td>
                                @*@item.Name*@
                            </td>
                            <td>
                                @*@item.Number*@
                            </td>
                        </tr>
                        @*}*@
                    </tbody>
                </table>
                @*@using (Html.BeginForm("Create", "Contact", FormMethod.Post, new { @class = "form-horizontal" }))
                    {*@
                <legend>New Entry</legend>
                <div class="row">
                    <div class="col-lg-6">
                        <label for="income" class="col-lg-2 control-label">Income</label>
                    </div>
                    <div class="col-lg-6">
                        <label for="expenses" class="col-lg-2 control-label">Expenses</label>
                    </div>
                </div>
                <div class="form-group">
                    <div class="row">
                        <div class="col-lg-3">
                            <input type="text" autofocus="autofocus" name="incomeDetail" placeholder="Income Details" class="form-control" id="incomeDetail" />
                        </div>
                        <div class="col-lg-3">
                            <input type="number" autofocus="autofocus" name="incomeValue" placeholder="Income Value" class="form-control" id="incomeValue" />
                        </div>
                        <div class="col-lg-3">
                            <input type="text" autofocus="autofocus" name="expenseDetail" placeholder="Expense Details" class="form-control" id="expenseDetail" />
                        </div>
                        <div class="col-lg-3">
                            <input type="number" autofocus="autofocus" name="expenseValue" placeholder="Expense Value" class="form-control" id="expenseValue" />
                        </div>
                    </div>

                </div>
                <div class="form-group">
                    <div class="row">
                        <div class="col-lg-6">
                            <button type="submit" formaction="Income" class="btn btn-primary">Add Income</button>
                        </div>
                        <div class="col-lg-6">
                            <button type="submit" formaction="Expense" class="btn btn-primary">Add Expense</button>
                        </div>
                    </div>

                </div>

                @*}*@
            </div>
        </div>
    </form>
</center>